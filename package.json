{
  "name": "the-bridge",
  "version": "0.1.0",
  "description": "A daemon for agent discovery and coordination across protocols and platforms.",
  "type": "module",
  "main": "src/daemon.js",
  "scripts": {
    "start": "node src/daemon.js",
    "dev": "node --watch src/daemon.js",
    "mcp": "node src/adapters/mcp.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "better-sqlite3": "^11.8.2",
    "zod": "^3.24.4"
  }
}
